name = "llmletsplay-docs"
compatibility_date = "2024-09-23"

# Asset Configuration
# Points to the site/ directory generated by MkDocs build
assets = { directory = "../../site" }

[observability]
enabled = true

# Production (Default)
# Domain: llmletsplay-docs.phantasybot.workers.dev
routes = [
	{ pattern = "docs.llmletsplay.com", custom_domain = true }
]
